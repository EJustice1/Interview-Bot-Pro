{
  "name": "user-auth-service-placeholder",
  "version": "0.0.1",
  "description": "Placeholder for User Auth Service",
  "main": "index.js",
  "scripts": {
    "start": "functions-framework --target=handleAuthRequest --port=8080", 
    "test": "echo \"Error: no test specified\" && exit 1"
  },
  "dependencies": {
    "firebase-admin": "^12.0.0" 
  },
  "devDependencies": {
    "@google-cloud/functions-framework": "^3.0.0" 
  },
  "engines": {
    "node": ">=18"
  }
}